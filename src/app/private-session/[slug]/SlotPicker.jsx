// DEPRECATED — the modal slot picker was replaced by the inline 3-step
// booking flow inside PractitionerView.jsx. No callers remain.
//
// Safe to delete this file. Kept as a stub here only because the sandbox
// that wrote the rewrite couldn't remove files; Mama can `git rm` it.
"use client";

export default function SlotPicker() {
  return null;
}
